# Download Dependencies
Use the requirements file for python dependencies: 
`pip3 install -r requirements.txt`

# Run Backend
To run backend, navigate to backend folder and run:
`fastapi dev main.py`
